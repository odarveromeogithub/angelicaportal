export const apiUrl = import.meta.env.VITE_API_ENDPOINT;
// export const apiClientId = import.meta.env.VITE_CLIENT_ID;
// export const apiClientSecret = import.meta.env.VITE_CLIENT_SECRET;

// Enable mock mode for testing (set to false when using real API)
export const USE_MOCK_DATA = import.meta.env.VITE_USE_MOCK_DATA === 'true' || true;